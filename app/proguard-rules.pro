# Add project specific ProGuard rules here.
# Retrofit / OkHttp / Gson / Room rules are added incrementally as those
# modules are wired in (Stage 6 onward) to keep this file meaningful rather
# than a wall of copy-pasted boilerplate.
